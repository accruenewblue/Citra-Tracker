import json
import tkinter
file=open(r"C:\Users\scien\Documents\GitHub\Orange-Peeler\mon-data","r+")
file2=open(r"C:\Users\scien\Documents\GitHub\Orange-Peeler\move-data")
data=json.load(file)
data2=json.load(file2)
mon="Ultra Necrozma"
datapoint="tag"
move="Dig"
attr="power"
dump={1:"gabbadegoo",
      2:"erertere"}
jsondumper=json.dumps(dump)
print(data[mon][datapoint])
print(data2[move])
print(jsondumper)
for item in range(0,4):
    print(item)
jsond=r"C:\Users\scien\Documents\GitHub\Orange-Peeler\mon-data"
    # Open and read the file, then convert json to a Python dictionary
with open(jsond, "r") as f:
    jsonData = json.loads(f.read())
print(jsonData)
    # Edit the Python dictionary
jsonData["Eevee"]["moves"].append("Tail Glow")

    # Convert Python dictionary to json and write to the file
with open(jsond, "w") as f:
    json.dump(jsonData, f)